import { assert } from 'chai'
import { describe, it } from 'mocha'
//import { RoomCategory, CategoryType } from './roomCategory'

describe('Room Category', () => {
    /* it('should create a valid category', () => {
        const validCategories = [
            CategoryType.GABINETE,
            CategoryType.ANFITEATRO,
            CategoryType.LABORATORIO,
            CategoryType.OUTRO,
        ]

        for (const category of validCategories) {
            const roomCategory = RoomCategory.create(category)
            assert.isOk(roomCategory.isSuccess)
        }
    })

    it('should create "OUTRO" for an unknown category', () => {
        const unknownCategory = 'UNKNOWN_CATEGORY'
        const roomCategory = RoomCategory.create(unknownCategory)

        assert.isOk(roomCategory.isSuccess)
    })*/
})
